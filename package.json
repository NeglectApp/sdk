{
  "name": "neglect",
  "version": "1.0.12",
  "description": "Neglect SDK — real-time market + token intelligence for Solana.",
  "author": "Neglect",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neglectapp/sdk.git"
  },
  "homepage": "https://github.com/neglectapp/sdk",
  "bugs": {
    "url": "https://github.com/neglectapp/sdk/issues"
  },
  "keywords": [
    "neglect",
    "solana",
    "sdk",
    "defi",
    "market-data",
    "trading",
    "web3",
    "tokens"
  ],
  "scripts": {
    "generate:sdk": "dotenv -e .env -- curl -H \"Authorization: Bearer $GITHUB_TOKEN\" -H \"Accept: application/vnd.github.raw+json\" -L \"https://api.github.com/repos/NeglectApp/api-gateway/contents/config/routes.oas.json?ref=main\" -o routes.oas.json && npx openapi --client fetch --input routes.oas.json --output src/generated",
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "dotenv -e .env -- npm run generate:sdk && npm run build",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^5.9.3"
  }
}
